# Datasource properties will be overridden by TestContainers
spring.datasource.url=
spring.datasource.username=
spring.datasource.password=

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update

# Optional: Show SQL in logs for debugging
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# Logging levels
#logging.level.org.springframework=INFO
#logging.level.com.petadoption=DEBUG

# Any other test-specific properties
spring.datasource.hikari.maximumPoolSize=100
spring.datasource.hikari.maxLifetime=10000
spring.datasource.hikari.connectionTimeout=20000
